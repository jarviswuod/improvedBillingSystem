spring:
  application:
    name: ImprovedBillingSystem
  config:
    activate:
      on-profile: prod

  datasource:
    url: jdbc:postgresql://localhost:5432/billing
    username: postgres
    password: "#Dot9047#Dot9047"
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

logging:
  level:
    root: warn
    org:
      springframework:
        data:
          postgresql: error
        web: error
    com:
      jarviswuod:
        improvedbillingsystem: info
